---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: coredns
spec:
  interval: 15m
  chart:
    spec:
      chart: coredns
      version: 1.19.2
      sourceRef:
        kind: HelmRepository
        name: coredns-charts
        namespace: flux-system

  valuesFrom:
    - kind: ConfigMap
      name: coredns-values
