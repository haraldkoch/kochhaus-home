apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
resources:
  - helm-release.yaml
configMapGenerator:
  - name: coredns-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
