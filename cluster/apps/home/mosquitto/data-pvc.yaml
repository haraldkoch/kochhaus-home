---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mosquitto-data-v2
  namespace: home
  labels:
    app.kubernetes.io/part-of: mosquitto
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
  storageClassName: ceph-block
