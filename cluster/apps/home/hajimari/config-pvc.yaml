---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hajimari-config-v3
  namespace: home
  labels:
    app.kubernetes.io/part-of: hajimari
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 128Mi
  storageClassName: zfs-nfs
