---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: miniflux
  namespace: default
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: miniflux-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        # Miniflux
        ADMIN_USERNAME: "{{ .ADMIN_USERNAME }}"
        ADMIN_PASSWORD: "{{ .ADMIN_PASSWORD }}"
        DATABASE_URL: "postgresql://{{ .POSTGRES_USER }}:{{ .POSTGRES_PASS }}@kochhaus-rw.dbms/miniflux?sslmode=disable"
        OAUTH2_CLIENT_SECRET: "{{ .MINIFLUX_OAUTH_CLIENT_SECRET }}" # from authelia
        # Postgres Init
        POSTGRES_DB: miniflux
        POSTGRES_HOST: kochhaus-rw.dbms.svc.cluster.local.
        POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
        POSTGRES_PASS: "{{ .OUTLINE_DATABASE_PASSWORD }}"
        POSTGRES_USER: "{{ .OUTLINE_DATABASE_USER }}"
  dataFrom:
    - extract:
        key: miniflux
    - extract:
        key: cloudnative-pg
    - extract:
        key: authelia
