---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sync-browser-config-v2
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
  storageClassName: zfs-nfs
