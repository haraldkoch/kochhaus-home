apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-assistant-pvc
  namespace: default
spec:
  resources:
    requests:
      storage: "1Gi"
  accessModes:
    - ReadWriteOnce
    - ReadOnlyMany
  storageClassName: zfs-nfs
