---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hajimari-config-v4
  namespace: default
  labels:
    app.kubernetes.io/part-of: hajimari
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 128Mi
  storageClassName: ceph-block
