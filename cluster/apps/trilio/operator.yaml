---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: trilio-operator
  namespace: trilio
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=http://charts.k8strilio.net/trilio-stable/k8s-triliovault-operator
      chart: k8s-triliovault-operator
      version: 2.6.4
      sourceRef:
        kind: HelmRepository
        name: triliovault-operator-charts
        namespace: flux-system
      interval: 5m
  values:
