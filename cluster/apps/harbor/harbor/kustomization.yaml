---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: harbor
resources:
  - helm-release.yaml
configMapGenerator:
  - name: harbor-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
