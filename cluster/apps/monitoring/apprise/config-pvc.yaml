---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: apprise-config-v1
  namespace: monitoring
  labels:
    app.kubernetes.io/part-of: apprise
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
  storageClassName: ceph-block
