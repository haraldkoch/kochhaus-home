---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tautulli-config-v3
  namespace: media
  labels:
    app.kubernetes.io/part-of: tautulli
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: zfs-nfs
