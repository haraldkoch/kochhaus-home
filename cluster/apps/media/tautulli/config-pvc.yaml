---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tautulli-config-v2
  namespace: media
  labels:
    app.kubernetes.io/part-of: tautulli
    kasten.io/backup-volume: "enabled"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1024Mi
  storageClassName: ceph-block
