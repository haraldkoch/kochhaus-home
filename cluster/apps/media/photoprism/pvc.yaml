---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoprism-config
  namespace: media
spec:
  storageClassName: zfs-nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 60Gi
