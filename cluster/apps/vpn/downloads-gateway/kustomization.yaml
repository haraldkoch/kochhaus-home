---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vpn
resources:
  - secret.sops.yaml
  - helm-release.yaml
configMapGenerator:
  - name: downloads-gateway-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
