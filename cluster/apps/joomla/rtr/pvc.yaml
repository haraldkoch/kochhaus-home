---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rtr-data-v1
  namespace: joomla
  labels:
    app.kubernetes.io/name: &name joomla
    app.kubernetes.io/instance: rtr
    app.kubernetes.io/part-of: joomla
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ceph-block
