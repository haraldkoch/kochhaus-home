---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-dns
  namespace: networking
spec:
  interval: 1h
  timeout: 30m
  chart:
    spec:
      # renovate: registryUrl=https://charts.bitnami.com/bitnami
      chart: external-dns
      version: 5.4.11
      sourceRef:
        kind: HelmRepository
        name: bitnami-charts
        namespace: flux-system

  values:
    provider: rfc2136
    rfc2136:
      host: ${SECRET_EXTERNAL_DNS_SERVER}
      zone: ${SECRET_DOMAIN}
      tsigSecret: ${SECRET_TSIG_KEY}
      tsigKeyname: externaldns
      tsigSecretAlg: hmac-sha256
    policy: sync
    sources:
      - service
      - ingress
      - crd
      # - istio-gateway
      # - istio-virtualservice
    domainFilters:
      - "${SECRET_DOMAIN}"
    createSecret: true
    crd:
      create: true
      apiversion: externaldns.k8s.io/v1alpha1
      kind: DNSEndpoint
#    metrics:
#      enabled: true
#      serviceMonitor:
#        enabled: true
    logLevel: debug
    resources:
      requests:
        memory: 250Mi
        cpu: 25m
      limits:
        memory: 250Mi
