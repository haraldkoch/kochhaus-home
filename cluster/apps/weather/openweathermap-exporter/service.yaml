apiVersion: v1
kind: Service
metadata:
  labels:
    app: openweathermap-exporter
  name: openweathermap-exporter
spec:
  ports:
  - name: openweathermap-exporter
    port: 2112
    targetPort: 2112
    protocol: TCP
  selector:
    app: openweathermap-exporter
