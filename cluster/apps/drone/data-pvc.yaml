---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drone-data-v1
  namespace: drone
  labels:
    app.kubernetes.io/part-of: drone
spec:
  resources:
    requests:
      storage: "8Gi"
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
