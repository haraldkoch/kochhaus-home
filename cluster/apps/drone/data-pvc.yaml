---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drone-data-v2
  namespace: drone
  labels:
    app.kubernetes.io/part-of: drone
spec:
  resources:
    requests:
      storage: "8Gi"
  accessModes:
    - ReadWriteOnce
  storageClassName: zfs-nfs
