---
k3s_cluster:
  children:
    master:
      hosts:
        k3sj:
      vars:
        ansible_user: ansible
        ansible_python_interpreter: /usr/bin/python
    worker:
      hosts:
        k3s0:
        k3s1:
        k3sm:
        k3s3:
      vars:
        ansible_user: ansible
        ansible_python_interpreter: /usr/bin/python
