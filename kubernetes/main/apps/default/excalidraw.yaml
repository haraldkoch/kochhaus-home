---
# yaml-language-server: $schema=https://datreeio.github.io/CRDs-catalog/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: excalidraw
spec:
  components:
    - ../../../../main/components/gatus/external
  interval: 1h
  path: ./kubernetes/base/apps/default/excalidraw
  postBuild:
    substitute:
      APP: excalidraw
      GATUS_SUBDOMAIN: draw
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: default
  wait: false
