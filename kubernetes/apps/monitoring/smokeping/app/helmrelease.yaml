---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app smokeping
  namespace: monitoring
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.0.3
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controller:
      annotations:
        reloader.stakater.com/auto: "true"
    image:
      repository: ghcr.io/linuxserver/smokeping
      tag: 2.8.2
    env:
      TZ: ${TIMEZONE}
    persistence:
      config:
        enabled: true
        existingClaim: smokeping-config-v1
      data:
        enabled: true
        existingClaim: smokeping-data-v4
      kochhaus:
        enabled: true
        type: secret
        name: *app
    initContainers:
      copy-config:
        image: ghcr.io/haraldkoch/kubernetes-kubectl:1.28.2@sha256:d53518a64baf7de0bb78ffc7ef3cd11a3bdb07f9143096998b2924d91877fbaf
        command:
          - /bin/sh
          - -c
          - |-
            /bin/cp /kochhaus/* /config
        volumeMounts:
          - name: config
            mountPath: /config
          - name: kochhaus
            mountPath: /kochhaus
        securityContext:
          runAsUser: 102
          runAsGroup: 101
    podSecurityContext:
      fsGroup: 101
      fsGroupChangePolicy: "OnRootMismatch"
    service:
      main:
        ports:
          http:
            port: 80
        ipFamilyPolicy: PreferDualStack
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        annotations:
          hajimari.io/icon: mdi:table-tennis
        hosts:
          - host: &host "{{ .Release.Name }}.${CLUSTER_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    resources:
      requests:
        cpu: 15m
        memory: 128Mi
      limits:
        cpu: 50m
        memory: 128Mi
