---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app etcd-defrag
spec:
  chartRef:
    kind: OCIRepository
    name: etcd-defrag
  interval: 1h
  values:
    controllers:
      main:
        type: cronjob
        cronjob:
          concurrencyPolicy: Forbid
          schedule: "2 2 * * 0"
        pod:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
        containers:
          main:
            image:
              repository: ghcr.io/haraldkoch/kubectl
              tag: 1.37.1@sha256:e98cc81d6e3502827933cd30a16da302f1e5991e309e6fc74b4fbca92e84eb16
            command:
              - /bin/sh
              - -xc
              - |-
                for node in $NODES ; do
                    /talosctl/talosctl etcd status -n $node
                    /talosctl/talosctl etcd defrag -n $node
                    /talosctl/talosctl etcd status -n $node
                done
            env:
              NODES: ${ETCD_NODES}
              TALOSCONFIG: /var/run/secrets/talos.dev/config
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
    persistence:
      talosctl:
        type: image
        image: ghcr.io/siderolabs/talosctl:v1.14.1
      talosconfig:
        type: secret
        name: etcd-defrag-talosconfig
        globalMounts:
          - path: /var/run/secrets/talos.dev
