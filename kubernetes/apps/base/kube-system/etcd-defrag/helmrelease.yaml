---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app etcd-defrag
spec:
  chartRef:
    kind: OCIRepository
    name: etcd-defrag
  interval: 1h
  values:
    controllers:
      main:
        type: cronjob
        cronjob:
          concurrencyPolicy: Forbid
          schedule: "2 2 * * 0"
        pod:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
        containers:
          main:
            image:
              repository: ghcr.io/haraldkoch/kubectl
              tag: 1.36.4@sha256:abe09fe3fb149ac1a6085cc79a4872fc006af0f302ae7ae6a9a583916f13cdcf
            command:
              - /bin/sh
              - -xc
              - |-
                for node in $NODES ; do
                    /talosctl/talosctl etcd status -n $node
                    /talosctl/talosctl etcd defrag -n $node
                    /talosctl/talosctl etcd status -n $node
                done
            env:
              NODES: ${ETCD_NODES}
              TALOSCONFIG: /var/run/secrets/talos.dev/config
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
    persistence:
      talosctl:
        type: image
        image: ghcr.io/siderolabs/talosctl:v1.13.9
      talosconfig:
        type: secret
        name: etcd-defrag-talosconfig
        globalMounts:
          - path: /var/run/secrets/talos.dev
