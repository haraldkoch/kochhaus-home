---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/refs/heads/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mealie
spec:
  chartRef:
    kind: OCIRepository
    name: mealie
  interval: 1h
  values:
    controllers:
      mealie:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18.4.0@sha256:5086f94abc783f1147d7c2a32c01db00ab594820026e4f6a82ac2af3dbde7fc7
            envFrom:
              - secretRef:
                  name: mealie-secret
        containers:
          mealie:
            image:
              repository: ghcr.io/mealie-recipes/mealie
              tag: v3.19.2@sha256:f68e959bf66f4f458893ea58facac71690fe6f2ac7a31466b5cecb41b4e99c02
            env:
              ALLOW_PASSWORD_LOGIN: "true"
              ALLOW_SIGNUP: "false"
              API_PORT: &port 9000
              BASE_URL: https://mealie.${CLUSTER_DOMAIN}
              DB_ENGINE: postgres
              OIDC_ADMIN_GROUP: mealie_admins
              OIDC_AUTH_ENABLED: "true"
              OIDC_CONFIGURATION_URL: https://auth.${CLUSTER_DOMAIN}/.well-known/openid-configuration
              OIDC_PROVIDER_NAME: Authelia
              OIDC_REMEMBER_ME: "true"
              OIDC_SIGNUP_ENABLED: "true"
              # OPENAI_MODEL: gpt-5-mini
              SMTP_HOST: smtp.${CLUSTER_DOMAIN}
              SMTP_PORT: "25"
              SMTP_FROM_EMAIL: mealie@${CLUSTER_DOMAIN}
              TZ: ${TIMEZONE}
            envFrom:
              - secretRef:
                  name: mealie-secret
            probes:
              liveness: &probe
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/app/about
                    port: *port
              readiness: *probe
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: {drop: ["ALL"]}
              readOnlyRootFilesystem: true
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 911
        runAsGroup: 911
        fsGroup: 911
        fsGroupChangePolicy: OnRootMismatch
    persistence:
      data:
        existingClaim: ${APP}
        advancedMounts:
          mealie:
            mealie:
              - path: /app/data
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
    route:
      mealie:
        hostnames:
          - mealie.${CLUSTER_DOMAIN}
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - identifier: mealie
                port: *port
    service:
      mealie:
        controller: mealie
        ports:
          http:
            port: *port
