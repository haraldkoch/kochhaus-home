---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: linode-backup
spec:
  chartRef:
    kind: OCIRepository
    name: linode-backup
  interval: 1h
  values:
    controllers:
      main:
        type: cronjob
        cronjob:
          concurrencyPolicy: Forbid
          schedule: "15 * * * *"
          failedJobsHistory: 2
        pod:
          labels:
            setGateway: "false"
            network: linode
          restartPolicy: Never
          securityContext:
            fsGroup: 1000
            runAsGroup: 1000
            runAsUser: 1000
        containers:
          main:
            image:
              repository: ghcr.io/haraldkoch/rsync
              tag: 3.5.0@sha256:348059fb21535cde82a52a3a47d4393123a51deb8643cde7f0f681884e1f0c38
            env:
              TZ: ${TIMEZONE:=Etc/UTC}
            command:
              - /bin/sh
              - -c
              - |-
                printf "\e[1;32m%-6s\e[m\n" "Running rsync ..."
                /usr/bin/rsync -avSAHX --numeric-ids -e "/usr/bin/ssh -i /ssh/id_rsa -o GlobalKnownHostsFile=/ssh/known_hosts" ${SECRET_WEB_BACKUP_USER}@${SECRET_WEB_HOST}:/data/website-backups/ /backup/
                printf "\e[1;32m%-6s\e[m\n" "rsync complete."
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
            resources:
              requests:
                memory: 128Mi
                cpu: 15m
              limits:
                memory: 512Mi
    persistence:
      backup:
        type: nfs
        server: ${SECRET_BACKUP_SERVER:=fnord}
        path: /backup/data/k8s/website-backups
      ssh:
        type: secret
        name: ssh-config
        globalMounts:
          - path: ssh
          - path: /etc/passwd
            subPath: passwd
            readOnly: true
