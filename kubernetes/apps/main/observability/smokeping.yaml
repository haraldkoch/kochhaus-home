---
# yaml-language-server: $schema=https://datreeio.github.io/CRDs-catalog/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: smokeping
spec:
  components:
    - ../../../../components/gatus/guarded
    - ../../../../components/main/volsync
  dependsOn:
    - name: volsync
      namespace: storage
  interval: 1h
  path: ./kubernetes/apps/base/observability/smokeping
  postBuild:
    substitute:
      APP: smokeping
      VOLSYNC_CAPACITY: 1Gi
      VOLSYNC_CLAIM: smokeping-data-v5
      VOLSYNC_GID: "101"
      VOLSYNC_UID: "102"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: false
