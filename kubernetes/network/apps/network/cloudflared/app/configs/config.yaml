---
originRequest:
  originServerName: network.${CLUSTER_DOMAIN}

ingress:
  - hostname: ${CLUSTER_DOMAIN}
    service: &service https://envoy-external.network.svc.cluster.local
  - hostname: "*.${CLUSTER_DOMAIN}"
    service: *service
  - service: http_status:404
